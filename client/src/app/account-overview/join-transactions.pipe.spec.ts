import { JoinTransactionsPipe } from './join-transactions.pipe';

describe('JoinTransactionsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinTransactionsPipe();
    expect(pipe).toBeTruthy();
  });
});
